# -*- coding: utf-8 -*-
"""
中學（國中）數學運算
=====================================================================
本檔案涵蓋國中階段常見的數學運算，並以 Python 程式碼示範，
每一段都會先說明數學原理，再提供程式範例與執行結果。

內容總覽：
  1. 四則運算（加減乘除）
  2. 指數與次方
  3. 平方根
  4. 絕對值
  5. 百分比
  6. 質數判斷
  7. 最大公因數（GCD）與最小公倍數（LCM）
  8. 圓周長與圓面積
  9. 畢氏定理（直角三角形）
"""

import math  # 匯入 math 模組，提供 sqrt、pi 等數學函式

print("=" * 70)
print("1. 四則運算（加法、減法、乘法、除法、整除、餘數）")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：四則運算是所有數學的基礎。
#   +  加法
#   -  減法
#   *  乘法
#   /  除法（結果為小數）
#   // 整數除法（只取商，捨去小數）
#   %  餘數（相除後剩下的部分）
# ---------------------------------------------------------------------
a, b = 10, 3
print(f"{a} + {b} = {a + b}")
print(f"{a} - {b} = {a - b}")
print(f"{a} * {b} = {a * b}")
print(f"{a} / {b} = {a / b:.4f}")
print(f"{a} // {b} = {a // b}   （10 除以 3 的商是 3）")
print(f"{a} % {b} = {a % b}    （10 除以 3 的餘數是 1）")

print()
print("=" * 70)
print("2. 指數與次方（**）")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：次方代表「連乘」。
#   2^3 = 2 × 2 × 2 = 8
# 在 Python 中用 ** 表示次方。
# ---------------------------------------------------------------------
base, exp = 2, 3
print(f"{base} 的 {exp} 次方 = {base ** exp} = {base} × {base} × {base}")

# 0 次方：任何非零數的 0 次方都等於 1
print(f"{base} 的 0 次方 = {base ** 0}")
# 負次方：2^-1 = 1/2 = 0.5
print(f"{base} 的 -1 次方 = {base ** -1}")

print()
print("=" * 70)
print("3. 平方根（math.sqrt）")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：平方根是「次方的相反運算」。
#   若 x² = 9，則 x = √9 = 3（正根）。
# Python 使用 math.sqrt(x) 計算平方根。
# ---------------------------------------------------------------------
x = 144
print(f"√{x} = {math.sqrt(x)}  （因為 {math.sqrt(x)}² = {x}）")

# 也可以透過 0.5 次方計算平方根
print(f"√{x} 的另一種寫法：{x} ** 0.5 = {x ** 0.5}")

print()
print("=" * 70)
print("4. 絕對值（abs）")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：絕對值代表「數到 0 的距離」，永遠不會是負數。
#   |5|  = 5
#   |-7| = 7
# ---------------------------------------------------------------------
num = -7
print(f"|{num}| = {abs(num)}")

print()
print("=" * 70)
print("5. 百分比（%）")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：百分比是「每 100 份中的幾份」。
#   80 分，滿分 100 分 → 80%
#   小數轉百分比：乘上 100
#   百分比轉小數：除以 100
# ---------------------------------------------------------------------
score, total = 85, 100
percent = score / total * 100
print(f"成績 {score} / {total} = {percent}%")

# 打折範例：原價 1200 元打 75 折
price = 1200
discount = 0.75
print(f"原價 {price} 元打 75 折 → {price * discount} 元")

print()
print("=" * 70)
print("6. 質數判斷")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：質數是「只能被 1 和自己整除」的數，例如 2、3、5、7、11。
#   1 不是質數。2 是最小且唯一的偶數質數。
# ---------------------------------------------------------------------
def is_prime(n: int) -> bool:
    """判斷 n 是否為質數。"""
    if n < 2:                    # 小於 2 的數不是質數
        return False
    for i in range(2, int(math.sqrt(n)) + 1):
        if n % i == 0:           # 找到其他因數 → 不是質數
            return False
    return True

for n in range(1, 21):
    if is_prime(n):
        print(f"{n} 是質數", end="  ")
print()

print()
print("=" * 70)
print("7. 最大公因數（GCD）與最小公倍數（LCM）")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：
#   最大公因數（GCD）：能同時整除兩數的最大整數。
#     例：12 與 18 的因數分別為 {1,2,3,6,12} 與 {1,2,3,6,9,18}
#         共同因數為 {1,2,3,6}，最大的是 6。
#   最小公倍數（LCM）：兩數共同的倍數中最小的一個。
#     公式：LCM(a, b) = a × b ÷ GCD(a, b)
# ---------------------------------------------------------------------
m, n = 12, 18
gcd = math.gcd(m, n)
lcm = m * n // gcd
print(f"GCD({m}, {n}) = {gcd}")
print(f"LCM({m}, {n}) = {lcm}")

print()
print("=" * 70)
print("8. 圓周長與圓面積")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：圓的基本公式（π 約等於 3.14159）。
#   圓周長 = 2 × π × 半徑
#   圓面積 = π × 半徑²
# Python 的 math.pi 提供精確的圓周率。
# ---------------------------------------------------------------------
r = 7
circumference = 2 * math.pi * r
area = math.pi * r ** 2
print(f"半徑 {r} 的圓：")
print(f"  圓周長 = 2 × π × {r} ≈ {circumference:.2f}")
print(f"  圓面積 = π × {r}² ≈ {area:.2f}")

print()
print("=" * 70)
print("9. 畢氏定理（直角三角形）")
print("=" * 70)
# ---------------------------------------------------------------------
# 說明：直角三角形中，斜邊² = 股² + 股²。
#   若兩股長為 3 與 4，則斜邊 c = √(3² + 4²) = √25 = 5。
# ---------------------------------------------------------------------
leg1, leg2 = 3, 4
hypotenuse = math.sqrt(leg1 ** 2 + leg2 ** 2)
print(f"兩股 {leg1}、{leg2} 的直角三角形，斜邊 = √({leg1}² + {leg2}²) = {hypotenuse}")
